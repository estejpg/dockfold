# Pull this project's environment with `vercel env pull .env.local`.
# Keep the actual file ignored; never commit credentials.
# BLOB_READ_WRITE_TOKEN comes from the connected PRIVATE DockFold store.
BLOB_READ_WRITE_TOKEN=
# Optional canonical origin when serving behind another proxy.
SITE_URL=https://dockfold.vercel.app
